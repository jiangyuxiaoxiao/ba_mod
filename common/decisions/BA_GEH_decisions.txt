GEH_emergency_time_decision_category = {

	#紧急工厂转换
	GEH_emergency_factory_change = {
		visible = {
		}
		available = {
			num_of_civilian_factories > 5
		}
		cost = 15
        days_re_enable = 60
		complete_effect = {
            add_stability = -0.05
			random_owned_controlled_state = {
				limit = { 
					non_damaged_building_level = {
						building = industrial_complex
						level > 0
					}
				}
				remove_building = {
					type = industrial_complex
					level = 1
				}
				add_building_construction = {
					type = arms_factory
					level = 1
					instant_build = yes
				}
			}

			random_owned_controlled_state = {
				limit = { 
					non_damaged_building_level = {
						building = industrial_complex
						level > 0
					}
				}
				remove_building = {
					type = industrial_complex
					level = 1
				}
				add_building_construction = {
					type = arms_factory
					level = 1
					instant_build = yes
				}
			}

			random_owned_controlled_state = {
				limit = { 
					non_damaged_building_level = {
						building = industrial_complex
						level > 0
					}
				}
				remove_building = {
					type = industrial_complex
					level = 1
				}
				add_building_construction = {
					type = arms_factory
					level = 1
					instant_build = yes
				}
			}

			random_owned_controlled_state = {
				limit = { 
					non_damaged_building_level = {
						building = industrial_complex
						level > 0
					}
				}
				remove_building = {
					type = industrial_complex
					level = 1
				}
				add_building_construction = {
					type = arms_factory
					level = 1
					instant_build = yes
				}
			}

			random_owned_controlled_state = {
				limit = { 
					non_damaged_building_level = {
						building = industrial_complex
						level > 0
					}
				}
				remove_building = {
					type = industrial_complex
					level = 1
				}
				add_building_construction = {
					type = arms_factory
					level = 1
					instant_build = yes
				}
			}
		}
    }


	#学园宵禁
	GEH_curfew_in_gehenna = {
		visible = {
		}
		available = {
		}
		cost = 15

		fire_only_once = yes

		complete_effect = {
			add_stability = 0.15
			add_war_support = -0.15
		}
	}


	#镇压混乱
	GEH_supress_the_choas = {
		visible = {
		}
		available = {
		}
		cost = 30

		fire_only_once = yes

		complete_effect = {
			add_stability = 0.1
			add_ideas = GEH_supress_the_choas_idea
		}
	}


	#交通管制
	GEH_control_the_transport = {
		visible = {
		}
		available = {
		}
		cost = 30

		fire_only_once = yes

		complete_effect = {
			add_stability = -0.05
			add_ideas = GEH_control_the_transport_idea
		}
	}


	#强行征召
	GEH_forecing_mobilization = {
		visible = {
		}
		available = {
		}
		cost = 50
		days_re_enable = 100

		complete_effect = {
			add_manpower = 50000
			add_stability = -0.1
			add_war_support = -0.1
		}
	}


	#死战不退
	GEH_no_step_back = {
		visible = {
		}
		available = {
		}
		cost = 50

		fire_only_once = yes

		complete_effect = {
			add_stability = -0.1
			add_war_support = -0.1
			add_ideas = GEH_no_step_back_idea
		}
	}


	###营救学生
	GEH_save_our_vip_students = {
		visible = {
			has_completed_focus = GEH_Emergency_Rescue
			has_dynamic_modifier = {
				modifier = GEH_a_unpredictable_attack
			}
		}
		available = {
			has_equipment = { infantry_equipment > 499 }
			has_equipment = { support_equipment > 99 }
			command_power > 24
			# NOT = {
			# 	check_variable = { 
			# 		var = GEH_save_students_count
			# 		value = 3
			# 		compare = greater_than_or_equals
			# 	}
			# }
		}
		cost = 15

		days_remove = 30

		complete_effect = {
			# custom_effect_tooltip = GEH_3_times_get_back_characters
			# hidden_effect = {
			# 	add_to_variable = {
			# 		var = GEH_save_students_count
			# 		value = 1
			# 	}
			# }
		}

		remove_effect = {
			add_command_power = -25
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = -500
			}
			add_equipment_to_stockpile = {
				type = support_equipment
				amount = -100
			}


			if = {
				limit = {
					check_variable = {
						GEH_a_unpredictable_attack_army_org_factor = -0.1
					}
				}
				remove_dynamic_modifier = {
					modifier = GEH_a_unpredictable_attack
				}
			}else = {
				custom_effect_tooltip = GEH_add_GEH_a_unpredictable_attack_ideas_changed_3_1_tt
			}

			


			hidden_effect = {
                add_to_variable = {
                    GEH_a_unpredictable_attack_army_org_factor = 0.1
                }
                add_to_variable = {
                    GEH_a_unpredictable_attack_army_morale_factor = 0.1
                }
                add_to_variable = {
                    GEH_a_unpredictable_attack_army_defence_factor = 0.1
                }
                add_to_variable = {
                    GEH_a_unpredictable_land_reinforce_rate = 0.02
                }      
            }
		}
	}


	#重新集结军队
	GEH_reorganization_our_troop = {
		visible = {
			has_completed_focus = GEH_Focus_attack
			NOT = {
				check_variable = { 
					var = GEH_regroup_troop_count
					value = 3
					compare = greater_than_or_equals
				}
			}
		}
		available = {
			NOT = {
				check_variable = { 
					var = GEH_regroup_troop_count
					value = 3
					compare = greater_than_or_equals
				}
			}
			command_power > 24
		}

		cost = 30
		days_remove = 30

		complete_effect = {
			add_command_power = -25
			hidden_effect = {
				add_to_variable = {
					var = GEH_regroup_troop_count
					value = 1
				}
			}
		}
		
		remove_effect = {
			GEH_degrade_debuff_of_troop = yes
		}
	}

}


GEH_after_eden_war_decision_category = {
	#停止产能动员
	GEH_stop_war_industry = {
		visible = {
			OR = {
				has_idea = GEH_war_industry_modifier
				has_idea = GEH_war_industry_2_modifier
			}
			
		}
		available = {
		}

		cost = 20
		days_remove = 35

		remove_effect = {
			if = {
				limit = { has_idea = GEH_war_industry_modifier }
				remove_ideas = GEH_war_industry_modifier
			}
			else = {
				remove_ideas = GEH_war_industry_2_modifier
			}
		}
		ai_will_do = {
			base = 0
		}
	}
}

GEH_dualHeaded = {
	GEH_dualHeaded_makoto_stay_calm = {
		visible = {
			
			has_completed_focus = GEH_Steady_progress
		}
		available = {
		}

		cost = 50
		days_remove = 20
		days_re_enable = 45


		modifier = {
			consumer_goods_factor = 0.1
		}

		complete_effect = {
			set_country_flag = GEH_dualHeaded_changing
		}

		remove_effect = {
			clr_country_flag = GEH_dualHeaded_changing
			  hidden_effect = {
                set_temp_variable = {
                    BARE_duelHead_Chance_temp = 0.05
                }
                add_to_variable = {
                    GEH_dualHeaded_Influence_Factor = BARE_duelHead_Chance_temp
                }
                GEH_dualHeaded_Influence_Factor_Change = yes

            }
            custom_effect_tooltip = GEH_dualHeaded_Influence_Factor_Change_Text_Right_Tooltip
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 2
				check_variable = {
					var = GEH_dualHeaded_Influence_Factor
					value = 0.4
					compare = less_than
				}
				not = {
					has_country_flag = GEH_dualHeaded_changing
				}
			}
		}
	}



	GEH_dualHeaded_makoto_take_hina_job = {
		visible = {
			has_completed_focus = GEH_Steady_progress
		}
		days_remove = 35
		days_re_enable = 180

		modifier = {
			GEH_dualHeaded_Makoto_Weekly_factor = 0.015
			power_balance_weekly = 0.015
		}

		custom_cost_trigger = {
			command_power > 40
		}

		custom_cost_text = custom_cost_GEH_dualHeaded_makoto_take_hina_job
		complete_effect = {
			add_command_power = -40
			set_country_flag = GEH_dualHeaded_changing
		}
		remove_effect = {
			clr_country_flag = GEH_dualHeaded_changing
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 2
				check_variable = {
					var = GEH_dualHeaded_Influence_Factor
					value = 0.4
					compare = less_than
				}
				not = {
					has_country_flag = GEH_dualHeaded_changing
				}
			}
		}
	}


	GEH_dualHeaded_makoto_publicity = {
		visible = {
			
			has_completed_focus = GEH_Steady_progress
		}
		available = {
			
		}

		days_remove = 28
		days_re_enable = 90

		modifier = {
			consumer_goods_factor = 0.5
			political_power_factor = 0.2
		}

		
		complete_effect = {
			set_country_flag = GEH_dualHeaded_changing
		}
		remove_effect = {
			clr_country_flag = GEH_dualHeaded_changing
			hidden_effect = {
                set_temp_variable = {
                    BARE_duelHead_Chance_temp = 0.075
                }
                add_to_variable = {
                    GEH_dualHeaded_Influence_Factor = BARE_duelHead_Chance_temp
                }
                GEH_dualHeaded_Influence_Factor_Change = yes

            }
            custom_effect_tooltip = GEH_dualHeaded_Influence_Factor_Change_Text_Right_Tooltip
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 2
				check_variable = {
					var = GEH_dualHeaded_Influence_Factor
					value = 0.4
					compare = less_than
				}
				not = {
					has_country_flag = GEH_dualHeaded_changing
				}
			}
		}
	}


	GEH_dualHeaded_makoto_send_away_hina = {
		visible = {
			
			has_completed_focus = GEH_Steady_progress
		}
		available = {
			
		}

		days_remove = 21

		fire_only_once = yes

		modifier = {
			political_power_factor = 0.5
			GEH_dualHeaded_Makoto_Weekly_factor = 0.05
			power_balance_weekly = 0.03
		}

		
		complete_effect = {
			set_country_flag = GEH_dualHeaded_changing
		}
		remove_effect = {
			clr_country_flag = GEH_dualHeaded_changing
			
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 2
				check_variable = {
					var = GEH_dualHeaded_Influence_Factor
					value = 0.4
					compare = less_than
				}
				not = {
					has_country_flag = GEH_dualHeaded_changing
				}
			}
		}
	}


	GEH_dualHeaded_makoto_send_away_hina = {
		visible = {
			
			has_completed_focus = GEH_Steady_progress
		}
		available = {
			
		}

		days_remove = 21

		fire_only_once = yes

		modifier = {
			political_power_factor = 0.5
			GEH_dualHeaded_Makoto_Weekly_factor = 0.05
			power_balance_weekly = 0.03
		}

		
		complete_effect = {
			set_country_flag = GEH_dualHeaded_changing
		}
		remove_effect = {
			clr_country_flag = GEH_dualHeaded_changing
			
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 2
				check_variable = {
					var = GEH_dualHeaded_Influence_Factor
					value = 0.4
					compare = less_than
				}
				not = {
					has_country_flag = GEH_dualHeaded_changing
				}
			}
		}
	}


	GEH_dualHeaded_makoto_alliance_with_sensei = {
		visible = {
			
			has_completed_focus = GEH_Steady_progress
		}
		available = {
		}

		cost = 50
		fire_only_once = yes


		modifier = {
			
		}

		complete_effect = {
			SCH = {
				country_event = {
					id = GEH_events.10
				}
			}
		}

		remove_effect = {
			  
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 2
				check_variable = {
					var = GEH_dualHeaded_Influence_Factor
					value = 0.4
					compare = less_than
				}
				not = {
					has_country_flag = GEH_dualHeaded_changing
				}
			}
		}
	}


	GEH_dualHeaded_hina_enforce_discipline = {
		visible = {
			
			has_completed_focus = GEH_Steady_progress
		}
		available = {
		
			
		}

		days_remove = 14
		days_re_enable = 90

		custom_cost_trigger = {
			has_equipment = { infantry_equipment > 500 }
			command_power > 30
		}

		custom_cost_text = custom_cost_GEH_dualHeaded_hina_enforce_discipline

		modifier = {
			
		}

		complete_effect = {
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = -500
			}
			add_command_power = -30
			set_country_flag = GEH_dualHeaded_changing
		}

		remove_effect = {
			army_experience = 15
			hidden_effect = {
                set_temp_variable = {
                    BARE_duelHead_Chance_temp = 0.05
                }
                subtract_from_variable = {
                    GEH_dualHeaded_Influence_Factor = BARE_duelHead_Chance_temp
                }
                GEH_dualHeaded_Influence_Factor_Change = yes
            }
			clr_country_flag = GEH_dualHeaded_changing
            custom_effect_tooltip = GEH_dualHeaded_Influence_Factor_Change_Text_Left_Tooltip
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 2
				check_variable = {
					var = GEH_dualHeaded_Influence_Factor
					value = 0.6
					compare = greater_than
				}
				not = {
					has_country_flag = GEH_dualHeaded_changing
				}
			}
		}
	}




	
	GEH_dualHeaded_hina_job_sharing = {
		visible = {
			
			has_completed_focus = GEH_Steady_progress
		}
		available = {
		
			
		}

		cost = 50

		days_re_enable = 120

		modifier = {
			
		}

		complete_effect = {
			
		}

		remove_effect = {
			hidden_effect = {
                set_temp_variable = {
                    BARE_duelHead_Chance_temp = 0.05
                }
                subtract_from_variable = {
                    GEH_dualHeaded_Influence_Factor = BARE_duelHead_Chance_temp
                }
                GEH_dualHeaded_Influence_Factor_Change = yes
            }

            custom_effect_tooltip = GEH_dualHeaded_Influence_Factor_Change_Text_Left_Tooltip
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 2
				check_variable = {
					var = GEH_dualHeaded_Influence_Factor
					value = 0.6
					compare = greater_than
				}
				not = {
					has_country_flag = GEH_dualHeaded_changing
				}
			}
		}
	}




	GEH_dualHeaded_hina_let_makoto = {
		visible = {
			
			has_completed_focus = GEH_Steady_progress
		}
		available = {
		
			
		}

		cost = 75

		days_remove = 28
		days_re_enable = 120

		modifier = {
			power_balance_weekly = 0.02
			GEH_dualHeaded_Hina_Weekly_factor = 0.03
		}

		complete_effect = {
			set_country_flag = GEH_dualHeaded_changing
		}

		remove_effect = {
			clr_country_flag = GEH_dualHeaded_changing
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 2
				check_variable = {
					var = GEH_dualHeaded_Influence_Factor
					value = 0.6
					compare = greater_than
				}
				not = {
					has_country_flag = GEH_dualHeaded_changing
				}
			}
		}
	}

		GEH_dualHeaded_hina_let_makoto = {
		visible = {
			
			has_completed_focus = GEH_Steady_progress
		}
		available = {
		
			
		}

		cost = 100

		days_re_enable = 210

		modifier = {

		}

		complete_effect = {
			
		}

		remove_effect = {
			add_stability = 0.03
			add_war_support = 0.03
			hidden_effect = {
                set_temp_variable = {
                    BARE_duelHead_Chance_temp = 0.1
                }
                subtract_from_variable = {
                    GEH_dualHeaded_Influence_Factor = BARE_duelHead_Chance_temp
                }
                GEH_dualHeaded_Influence_Factor_Change = yes
            }

            custom_effect_tooltip = GEH_dualHeaded_Influence_Factor_Change_Text_Left_Tooltip
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 2
				check_variable = {
					var = GEH_dualHeaded_Influence_Factor
					value = 0.6
					compare = greater_than
				}
				not = {
					has_country_flag = GEH_dualHeaded_changing
				}
			}
		}
	}

	
}